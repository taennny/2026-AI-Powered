# Roame 테스트 가이드

앱을 아이폰에서 돌려보는 방법을 정리했습니다. 크게 두 가지입니다.

|                  | 방식                                       | 노트북 필요?       | 언제 쓰나                                        |
| ---------------- | ------------------------------------------ | ------------------ | ------------------------------------------------ |
| **개발 빌드**    | Metro(노트북)에서 코드를 실시간으로 받아옴 | **켜져 있어야 함** | 개발 중. 코드 고치면 바로 반영, 로그도 보임      |
| **Release 빌드** | 코드가 앱 안에 들어감                      | 설치할 때만        | 며칠 들고 다니며 테스트. 실제 사용자와 같은 환경 |

처음이라면 **1 → 2 → 3** 순서로 따라오세요.

---

## 0. 사전 준비

- macOS + Xcode (App Store에서 설치)
- Node.js 18 이상
- Docker Desktop (백엔드를 로컬에서 띄울 때만)
- 아이폰과 연결할 케이블

```bash
git clone https://github.com/taennny/2026-AI-Powered.git
cd 2026-AI-Powered

# 환경변수 파일 생성 (실제 키는 팀 채널에서 받아 채우세요)
cd backend && cp .env.example .env && cd ..
cd frontend && cp .env.example .env && cd ..

cd frontend
npm install
cd ios && bundle exec pod install && cd ..
```

> `EXPO_PUBLIC_API_BASE_URL`은 **끝에 슬래시를 붙이지 마세요.**
> 붙이면 카카오 로그인이 막힙니다 (redirect_uri가 등록값과 달라짐).

---

## 1. 아이폰 연결하기 (처음 한 번만, 케이블 연결 경험이 있다면 바로 2번으로)

### 1-1. 케이블로 연결(충전용 불가함)

1. 아이폰을 맥에 케이블로 연결합니다
2. 아이폰에 **"이 컴퓨터를 신뢰하시겠습니까?"** 가 뜨면 → **신뢰** → 암호 입력
3. 아이폰에서 **설정 > 개인정보 보호 및 보안 > 개발자 모드** → 켬 → 재시동
   - 이 메뉴는 맥에 한 번 연결한 뒤에 나타납니다

### 1-2. Xcode에 애플 계정 등록

앱을 아이폰에 설치하려면 서명이 필요합니다. 무료 계정으로도 됩니다.

1. Xcode 실행 → **Settings > Accounts** → `+` → Apple ID 로그인
2. `ios/roame.xcworkspace`를 Xcode로 열기
3. 왼쪽에서 프로젝트 선택 → **Signing & Capabilities** 탭
4. **Automatically manage signing** 체크 → **Team**에 방금 추가한 계정 선택

> 무료 계정은 **7일마다 앱이 만료**됩니다. 만료되면 다시 설치하면 됩니다.
> Apple Developer Program($99/년)이 있으면 1년입니다.

### 1-3. 설치 후 "신뢰할 수 없는 개발자" 뜰 때

아이폰에서 **설정 > 일반 > VPN 및 기기 관리** → 본인 Apple ID → **신뢰**

---

## 2. 개발 빌드 — 코드 고치면서 테스트 (실제 사용 테스트는 3번으로)

Metro(노트북)가 켜져 있어야 앱이 돌아갑니다.

### 2-1. 케이블 연결 상태로 설치(케이블 연결 경험이 있다면 연결하지 않은 채 명령어 실행한 뒤 본인 휴대폰 이름 선택)

```bash
cd frontend
npx expo run:ios --device
```

연결된 기기 목록이 나오면 본인 아이폰을 고릅니다. 빌드에 몇 분 걸리고, 끝나면 앱이 자동으로 실행됩니다.

### 2-2. 그다음부터는 케이블 없이 (같은 와이파이)

**한 번 설치해두면 케이블을 빼도 됩니다.** 맥과 아이폰이 같은 와이파이에 있으면 됩니다.

```bash
cd frontend
npx expo start
```

아이폰에서 앱을 열면 Metro에 알아서 붙습니다. 안 붙으면 앱을 흔들어 개발 메뉴 → **Reload**.

> 회사·학교 와이파이는 기기 간 통신이 막혀 있는 경우가 있습니다.
> 그럴 때는 케이블을 연결한 채로 쓰거나, 맥의 핫스팟에 아이폰을 붙이면 됩니다.

### 2-3. 코드를 고쳤는데 반영이 안 될 때

```bash
npx expo start -c        # Metro 캐시 초기화
```

`.env`를 고쳤다면 **반드시 `-c`로 재시작**해야 합니다. 환경변수는 번들에 값이 박히기 때문입니다.

### 2-4. 로그 보기

`console.log`와 에러는 **`npx expo start`를 실행한 터미널에 그대로 찍힙니다.** 별도 디버거를 열 필요 없습니다.

앱에서 **기기를 흔들면** 개발 메뉴가 뜹니다:

| 메뉴                     | 하는 일                                               |
| ------------------------ | ----------------------------------------------------- |
| Reload                   | 번들 다시 받기                                        |
| Toggle Element Inspector | 화면 요소의 위치·스타일 확인                          |
| Open JS Debugger         | 크롬 개발자 도구 연결 (네트워크 요청, 브레이크포인트) |
| Show Perf Monitor        | FPS·메모리 표시                                       |

### 2-5. 네이티브 모듈이 추가됐을 때

`package.json`에 네이티브 패키지가 들어오면 **Metro 재시작만으로는 안 되고 다시 설치해야 합니다.**

```bash
npm install
cd ios && bundle exec pod install && cd ..
npx expo run:ios --device
```

안 하면 `Cannot find native module 'XXX'` 에러가 납니다.

---

## 3. Release 빌드 — 노트북 없이 며칠 써보기 (실사용 테스트)

코드가 앱 안에 들어가서 **Metro 없이 혼자 돌아갑니다.** 실제 사용자와 같은 조건이라 최종 확인용으로 적합합니다.

```bash
cd frontend
npx expo run:ios --configuration Release --device
```

설치가 끝나면 케이블을 빼고 그냥 쓰면 됩니다.

**개발 빌드와 다른 점:**

- 훨씬 빠릅니다 (개발 모드 오버헤드가 없음)
- `console.log`·에러 화면이 안 나옵니다
- 코드나 `.env`를 고쳐도 반영되지 않습니다 → 다시 빌드해야 합니다
- 무료 계정이면 7일 뒤 만료됩니다

---

## 4. 백엔드

기본값은 배포된 서버(`https://api.roame.co.kr`)를 봅니다. 로컬 백엔드로 붙이려면 `frontend/.env`를 고칩니다.

```bash
# 루트에서
docker compose up --build
```

http://localhost:8000/docs 에서 API를 직접 호출해볼 수 있습니다.

```
# frontend/.env — 실기기에서 로컬 백엔드를 보려면 맥의 IP를 적습니다
# (localhost는 아이폰 자신을 가리켜서 안 됩니다)
EXPO_PUBLIC_API_BASE_URL=http://192.168.0.10:8000
```

맥 IP는 `ipconfig getifaddr en0`으로 확인합니다.

```bash
docker compose exec backend pytest       # 백엔드 테스트
```

---

## 5. 프론트엔드 테스트

```bash
cd frontend
npm test              # 전체
npm run test:watch    # 변경 감지
npx jest photoSync    # 파일 하나
npx tsc --noEmit      # 타입 검사
npm run lint
```

화면 렌더링은 테스트하지 않고 순수 로직만 다룹니다. `jest.setup.js`가 타임존을 `Asia/Seoul`로 고정합니다.

---

## 6. 기능별 확인 방법

| 기능             | 확인 방법                                                             |
| ---------------- | --------------------------------------------------------------------- |
| 위치 기록        | 앱을 켜둔 채 밖에서 3분 이상 머물면 타임라인에 카드가 생깁니다        |
| 위치 기록 끄기   | 설정 > 기록 > 위치 기록. 끄면 헤더 왼쪽에 "위치 기록 꺼짐"이 뜹니다   |
| 지도 미리보기    | 바텀시트를 위로 올리면 나타납니다                                     |
| 지도 공유        | 지도를 0.5초 꾹 누르면 확대 모달 → 공유                               |
| 캘린더 월 이동   | 좌우로 스와이프하거나 `< >` 버튼                                      |
| 사진 자동 업로드 | **와이파이에서만** 동작합니다. 사진을 찍고 앱으로 돌아오면 올라갑니다 |
| 저널 검색        | 제목·본문·날짜(`26.08`, `27`, `thu`) 모두 검색됩니다                  |
| 카카오 로그인    | 실기기에서만 됩니다 (웹·시뮬레이터 불가 — 아래 참고)                  |

---

## 알아둘 것

| 항목                                    | 내용                                                                                                                          |
| --------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------- |
| **카카오 로그인이 웹에서 안 되는 이유** | 백엔드가 마지막에 `roameapp://` 딥링크로 리다이렉트하는데, 브라우저는 이 주소를 열 수 없습니다. **실기기에서만** 테스트하세요 |
| **Expo Go**                             | 백그라운드 GPS·사진 라이브러리를 지원하지 않습니다. 개발 빌드를 쓰세요                                                        |
| **사진이 안 올라갈 때**                 | 셀룰러에서는 올라가지 않습니다(데이터 절약). 와이파이인지 확인하세요                                                          |
| **지도가 회색 박스일 때**               | `.env`의 `EXPO_PUBLIC_GOOGLE_MAPS_KEY`가 비어 있습니다                                                                        |
| **7일 뒤 앱이 안 열릴 때**              | 무료 애플 계정의 서명 만료입니다. 다시 설치하면 됩니다                                                                        |
| **`Cannot find native module`**         | 네이티브 패키지가 추가됐습니다. 2-5를 따라 다시 설치하세요                                                                    |
| **백그라운드 GPS 끄기**                 | `.env`의 `EXPO_PUBLIC_BG_GPS=false` (Expo Go로 열 때)                                                                         |
