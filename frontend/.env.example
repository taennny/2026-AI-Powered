# .env.example — 이 파일은 git에 올라갑니다 (키 값 없이 형식만 공유)
# 실제 키는 팀 내부 채널에서 공유받아 .env 파일에 입력하세요

# 로컬 백엔드로 붙을 때는 http://localhost:8000
# 맨 끝에 슬래시 붙이지 말기 (카카오 redirect_uri가 등록값과 한 글자라도 다르면 로그인 자체가 막힘)
EXPO_PUBLIC_API_BASE_URL=https://api.roame.co.kr

EXPO_PUBLIC_GOOGLE_MAPS_KEY=your_google_maps_api_key_here

# 카카오 로그인. 개발자 콘솔의 REST API 키
# 리다이렉트 URI는 EXPO_PUBLIC_API_BASE_URL에서 자동으로 만들어진다 (constants/kakao.ts)
EXPO_PUBLIC_KAKAO_REST_API_KEY=your_kakao_rest_api_key_here

# RevenueCat 공개 API 키 (대시보드 > API keys의 public 키. secret 키를 넣지 말 것)
# 없으면 결제 기능만 조용히 꺼지고 앱은 그대로 동작한다
EXPO_PUBLIC_REVENUECAT_IOS_KEY=appl_your_public_sdk_key_here
EXPO_PUBLIC_REVENUECAT_ANDROID_KEY=goog_your_public_sdk_key_here
