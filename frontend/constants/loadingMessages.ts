/**
 * AI 글 생성을 기다리는 동안 보여주는 문구.
 *
 * 정지 화면으로 40초를 버티면 멈춘 것처럼 보인다 — 문구가 바뀌는 것이
 * "살아 있다"는 유일한 신호다.
 *
 * 두 종류를 **번갈아** 내보낸다 (`utils/loadingSequence.ts`가 엮는다).
 *   진행 안내 → 튜토리얼 → 진행 안내 → 튜토리얼 … → 마무리
 */

/**
 * 진행 안내 — **순서에 의미가 있다.** 지금 뭘 하는 중인지 알려 남은 시간의 감을 준다.
 * 섞으면 "다듬는 중" 다음에 "살펴보는 중"이 나와 거꾸로 간 것처럼 보인다.
 */
export const PROGRESS_MESSAGES = [
  '로미가 열심히 적고 있어요.',
  '그날 다녀온 곳을 살펴보는 중이에요.',
  '찍은 사진의 조도를 확인하고 있어요.',
  '시간 순서로 이야기를 엮고 있어요.',
  '문장을 예쁘게 다듬는 중이에요.',
] as const;

/**
 * 튜토리얼 — **순서가 없다. 매번 섞어서 내보낸다.**
 * 생성이 5초 만에 끝나는 사람은 앞의 한두 개밖에 못 보는데,
 * 순서가 고정이면 그 사람은 평생 같은 문구만 본다.
 *
 * 여기 적는 내용은 실제 동작과 맞아야 한다 — 화면을 바꿀 때 이 목록도 같이 본다.
 */
export const TIP_MESSAGES = [
  '홈 화면 위쪽 새로고침 버튼을 누르면 기록을 바로 갱신할 수 있어요.',
  '기록을 멈추고 싶을 땐 설정 > 기록에서 잠시 끌 수 있어요.',
  '지도를 꾹 누르면 사진으로 저장하거나 공유할 수 있어요.',
  '달력에서 날짜를 꾹 누르면 여러 날을 한 편으로 묶어 쓸 수 있어요.',
  '구독하면 글쓰기 횟수 제한 없이, 테마도 모두 쓸 수 있어요.',
  '베타 테스트에서는 모든 기능을 제한 없이 사용할 수 있어요.',
] as const;

/**
 * 맨 마지막에 한 번. 여기서 멈춘 채로 남는다.
 *
 * 마지막이 튜토리얼이면 예상보다 오래 걸릴 때 화면에 광고가 남는다.
 * 기다리는 사람에게는 안심시키는 말이 마지막에 있어야 한다.
 */
export const CLOSING_MESSAGE = '거의 다 됐어요. 조금만 기다려주세요.';

/**
 * 말줄임표가 한 점씩 늘어나는 간격 (`.` → `..` → `...`).
 * 300ms 아래로 내리면 깜빡이는 것처럼 보이고, 1초를 넘기면 멈춘 것 같다.
 */
export const DOT_STEP_MS = 500;

/**
 * 점이 세 개까지 차오르기를 몇 번 반복한 뒤 다음 문구로 넘어가는지.
 *
 * 한 문구가 머무는 시간 = `DOT_STEP_MS * 3 * 이 값` = **3초**.
 * 한 번(1.5초)이면 다 읽기 전에 넘어가고, 세 번(4.5초)이면 멈춘 것처럼 보인다.
 */
export const DOT_CYCLES_PER_MESSAGE = 2;

/** 교체할 때 페이드. 글자가 툭 바뀌면 산만하다 */
export const MESSAGE_FADE_MS = 200;
