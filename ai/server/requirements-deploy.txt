# 배포용 슬림 의존성 (EC2). torch/transformers/pillow 제외 → 이미지 경량화.
# CLIP(AI-2 분류)은 아직 백엔드 미연동이라 제외해도 classify 는 매칭만 graceful 동작.
# 로컬에서 CLIP 테스트하려면 requirements.txt(전체)를 쓸 것.
flask
flask-restx
flask-cors
python-dotenv
# AI-1 (GPS 체류 감지 + 카카오 장소 매칭)
geopy
pandas
requests
# AI-3 (GPT 블로그 생성)
openai
# 프로덕션 WSGI 서버
gunicorn
