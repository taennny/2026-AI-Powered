# 개발·CI 전용 의존성. 운영 이미지에는 설치하지 않는다.
-r requirements.txt

pytest==8.3.5
pytest-asyncio==0.26.0
aiosqlite==0.21.0
ruff==0.11.2
black==25.1.0
# 테스트 픽스처(JPEG) 생성용 — 운영 코드는 EXIF를 piexif로 읽는다
Pillow==11.2.1
